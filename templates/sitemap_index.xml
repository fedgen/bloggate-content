<?xml version="1.0" encoding="UTF-8"?>

<sitemapindex xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">

  {% for number in range(1, pages + 1) %}
    <sitemap>
      <loc>{{ "{0}/sitemap/{1}".format(base_url, number) }}</loc>
    </sitemap>
  {% endfor %}

</sitemapindex>
